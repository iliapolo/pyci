branches:
  except:
    - master

skip_tags: true

environment:

  matrix:

    - PYTHON: "C:\\Python27"
      TOX_ENV: test-py27,release

    - PYTHON: "C:\\Python36"
      TOX_ENV: test-py36,release

build: off

install:
  - cmd: "%PYTHON%\\Scripts\\pip.exe install tox==3.0.0"

test_script:
  - cmd: "%PYTHON%\\Scripts\\tox.exe -e %TOX_ENV%"
